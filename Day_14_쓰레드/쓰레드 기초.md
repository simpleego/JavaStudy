# ✨ 자바의 **쓰레드(Thread)**

---

## 🧩 자바 쓰레드(Thread) 정리

### 1. 쓰레드란?
- **정의**: 프로그램 내에서 **동시에 실행되는 흐름**(실행 단위).  
- **목적**: 멀티태스킹, 병렬 처리, 비동기 작업 수행.  
- **프로세스 vs 쓰레드**  
  - 프로세스: 실행 중인 프로그램 전체  
  - 쓰레드: 프로세스 내에서 실행되는 작은 단위  

---

### 2. 쓰레드 생성 방법
1. **Thread 클래스 상속**
   - `Thread` 클래스를 상속받아 `run()` 메서드 오버라이딩  
   - `start()` 메서드로 실행  

2. **Runnable 인터페이스 구현**
   - `Runnable` 인터페이스의 `run()` 메서드 구현  
   - `Thread` 객체에 전달하여 실행  

---

### 3. 주요 메서드
- `start()` → 쓰레드 실행 시작 (내부적으로 `run()` 호출)  
- `run()` → 쓰레드가 수행할 작업 정의  
- `sleep(ms)` → 일정 시간 동안 쓰레드 일시 정지  
- `join()` → 다른 쓰레드가 종료될 때까지 대기  
- `interrupt()` → 쓰레드 실행 중단 요청  

---

## 💻 예제 코드

### 방법 1: Thread 클래스 상속
```java
class MyThread extends Thread {
    public void run() {
        for (int i = 1; i <= 5; i++) {
            System.out.println("MyThread 실행: " + i);
            try {
                Thread.sleep(500); // 0.5초 대기
            } catch (InterruptedException e) {
                System.out.println("쓰레드 중단됨");
            }
        }
    }
}

public class ThreadExample1 {
    public static void main(String[] args) {
        MyThread t = new MyThread();
        t.start(); // 쓰레드 실행
        System.out.println("메인 쓰레드 실행 중...");
    }
}
```

---

### 방법 2: Runnable 인터페이스 구현
```java
class MyRunnable implements Runnable {
    public void run() {
        for (int i = 1; i <= 5; i++) {
            System.out.println("Runnable 실행: " + i);
            try {
                Thread.sleep(500);
            } catch (InterruptedException e) {
                System.out.println("쓰레드 중단됨");
            }
        }
    }
}

public class ThreadExample2 {
    public static void main(String[] args) {
        Thread t = new Thread(new MyRunnable());
        t.start(); // 쓰레드 실행
        System.out.println("메인 쓰레드 실행 중...");
    }
}
```

---

## ⚡ 실행 결과 예시
```
메인 쓰레드 실행 중...
MyThread 실행: 1
MyThread 실행: 2
...
```
또는
```
메인 쓰레드 실행 중...
Runnable 실행: 1
Runnable 실행: 2
...
```

---

## ✅ 핵심 포인트
- `start()`를 호출해야 **새로운 쓰레드**가 실행됨 (`run()` 직접 호출은 단일 흐름).  
- 멀티쓰레드 환경에서는 **동기화(synchronization)** 문제를 주의해야 함.  
- CPU 코어 수에 따라 실제 병렬 실행 여부가 달라질 수 있음.  

---
