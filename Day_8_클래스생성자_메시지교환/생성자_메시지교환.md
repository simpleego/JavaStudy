# 생성자와 객체간의 메시지 교환
좋아 jong, **자바의 생성자, this(), 생성자 오버로딩, 객체 간 메시지 교환**을 한 번에 이해할 수 있는 깔끔한 예제를 정리해줄게.  
학생들에게 설명하기에도 딱 좋은 구조로 구성했어.

---

# 🎯 **1. 생성자(Constructor)란?**

객체가 생성될 때 자동으로 호출되는 **초기화 메서드**  
- 클래스 이름과 동일  
- 반환 타입 없음  
- 객체의 초기 상태를 설정하는 역할

```java
public class Person {
    String name;
    int age;

    // 생성자
    public Person(String name, int age) {
        this.name = name;
        this.age = age;
    }
}
```

---

# 🎯 **2. this 키워드**

### ✔ this.변수  
- 객체 자신의 멤버 변수에 접근할 때 사용  
- 매개변수 이름과 멤버 변수 이름이 같을 때 구분하는 역할

### ✔ this()  
- **같은 클래스의 다른 생성자를 호출**할 때 사용  
- 생성자 오버로딩에서 중복 코드 제거에 매우 유용

---

# 🎯 **3. 생성자 오버로딩(Constructor Overloading)**

같은 이름의 생성자를 여러 개 만들되,  
**매개변수의 개수 또는 타입이 다르면 허용**되는 문법

예:

```java
public Person() { }               // 기본 생성자
public Person(String name) { }    // 이름만 받는 생성자
public Person(String name, int age) { } // 이름 + 나이 생성자
```

---

# 🎯 **4. 객체 간 메시지 교환이란?**

객체가 다른 객체의 메서드를 호출하여 **명령을 전달하는 것**  
→ 객체지향의 핵심 개념

예:  
- Student 객체가 Teacher 객체에게 질문한다  
- Remote 객체가 TV 객체에게 전원 켜기 명령을 보낸다  
- Car 객체가 Engine 객체를 작동시킨다

---

# 🌟 **5. 전체 개념을 모두 포함한 예제**

아래 예제는 다음을 모두 포함한다:

- 생성자  
- this()  
- 생성자 오버로딩  
- 객체 간 메시지 교환  

---

# ✅ **예제: 사람(Person)과 스마트폰(Phone)의 상호작용**

## 📌 Person.java

```java
public class Person {
    private String name;
    private int age;
    private Phone phone;

    // 기본 생성자
    public Person() {
        this("이름없음", 0);   // this()로 다른 생성자 호출
    }

    // 이름만 받는 생성자
    public Person(String name) {
        this(name, 0);         // this()로 또 다른 생성자 호출
    }

    // 이름 + 나이 생성자 (실제 초기화 담당)
    public Person(String name, int age) {
        this.name = name;
        this.age = age;
    }

    // 스마트폰을 소유하는 메서드
    public void buyPhone(Phone phone) {
        this.phone = phone;
        System.out.println(name + "이(가) " + phone.getModel() + "을(를) 구매했습니다.");
    }

    // 스마트폰에게 메시지를 보내는 메서드 (객체 간 메시지 교환)
    public void call(String target) {
        if (phone == null) {
            System.out.println(name + "은(는) 아직 스마트폰이 없습니다.");
            return;
        }
        phone.call(target); // Phone 객체에게 메시지 전달
    }
}
```

---

## 📌 Phone.java

```java
public class Phone {
    private String model;

    public Phone(String model) {
        this.model = model;
    }

    public String getModel() {
        return model;
    }

    // 전화 걸기 기능
    public void call(String target) {
        System.out.println(model + " → " + target + "에게 전화를 겁니다.");
    }
}
```

---

## 📌 Main.java

```java
public class Main {
    public static void main(String[] args) {

        // 생성자 오버로딩 테스트
        Person p1 = new Person();
        Person p2 = new Person("홍길동");
        Person p3 = new Person("김철수", 25);

        // 객체 간 메시지 교환
        Phone galaxy = new Phone("Galaxy S24");

        p3.buyPhone(galaxy);   // 사람 → 스마트폰에게 메시지 전달
        p3.call("엄마");       // 사람 → 스마트폰 → 대상
    }
}
```

---

# 🎉 **6. 이 예제로 학생들이 배우는 핵심 개념**

| 개념 | 예제에서의 위치 |
|------|----------------|
| 생성자 | Person(), Person(String), Person(String,int) |
| this() | 기본 생성자와 이름만 받는 생성자에서 사용 |
| 생성자 오버로딩 | Person 생성자 3개 |
| 객체 간 메시지 교환 | Person → Phone.call() 호출 |
| 캡슐화 | private 멤버 + public 메서드 |
| 객체 생성 | new Person(), new Phone() |

---
