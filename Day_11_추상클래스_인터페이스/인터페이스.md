# 인터페이스
- 객체(부품)을 만드는 표준 규격을 정의하는 것
- 메서드를 구현하지 않고, 메서드의 시그니쳐(메서드 이름, 매개변수, 반환값)만을 정의
- 인터페이스는 다른 클래스에서 구현하여 사용하도록 함
> 타이어를 만드는데 표준 규격대로 만들면 전 세계의 모든 자동차에 사용할 수 있는 것
> 즉 규격을 일치시켜 모두가 사용하는데 편리하게 해주는 표준화 기술이라 보면 된다.
> 인퍼페이스를 따르지 않는 객체는 결국 재사용성이 떨어지게 된다.

---

# 🧩 1. **인터페이스(interface)란?**

인터페이스는 **“기능(행동)의 규칙만 정의하는 설계도”** 야.  
즉, **무엇을 해야 하는지(메서드 이름)** 만 정하고  
**어떻게 하는지(구현)** 는 전혀 적지 않아.

### ✔ 인터페이스의 특징
- `interface` 키워드로 선언
- **모든 메서드는 기본적으로 추상 메서드**
- **필드는 자동으로 public static final(상수)**
- **다중 구현 가능** → 클래스는 여러 인터페이스를 implements 할 수 있음
- 객체 생성 불가 (추상클래스와 동일)

---

# 🎯 2. 인터페이스를 사용하는 목적

### ✔ (1) “규칙”을 강제하기 위해  
예: 모든 결제 수단은 `pay()` 기능을 가져야 한다.

### ✔ (2) 서로 다른 클래스들을 같은 방식으로 다루기 위해  
예: USB 장치는 종류가 달라도 `connect()` 기능을 제공해야 한다.

### ✔ (3) 다중 상속 문제를 해결하기 위해  
자바는 클래스 다중 상속은 안 되지만  
**인터페이스는 여러 개 구현 가능** → 유연한 설계 가능

---

# 🚀 3. 인터페이스 예제: **날 수 있는(Flyable) 능력**

비행기, 새, 드론은 모두 “날 수 있다”는 공통 능력이 있지만  
상속 구조는 서로 완전히 다를 수 있어.

이럴 때 **인터페이스가 가장 적절**해.

---

## 🔹 Flyable 인터페이스

```java
interface Flyable {
    void fly();  // 날다 기능 (규칙만 정의)
}
```

---

## 🔹 비행기 Airplane

```java
class Airplane implements Flyable {
    @Override
    public void fly() {
        System.out.println("비행기가 엔진을 이용해 하늘을 납니다.");
    }
}
```

---

## 🔹 새 Bird

```java
class Bird implements Flyable {
    @Override
    public void fly() {
        System.out.println("새가 날개를 퍼덕이며 하늘을 납니다.");
    }
}
```

---

## 🔹 드론 Drone

```java
class Drone implements Flyable {
    @Override
    public void fly() {
        System.out.println("드론이 프로펠러로 공중에 떠오릅니다.");
    }
}
```

---

## 🔹 테스트 코드

```java
public class FlyTest {
    public static void main(String[] args) {
        Flyable[] items = {
            new Airplane(),
            new Bird(),
            new Drone()
        };

        for (Flyable f : items) {
            f.fly();  // 다형성
        }
    }
}
```

---

# 🎉 출력 예시

```
비행기가 엔진을 이용해 하늘을 납니다.
새가 날개를 퍼덕이며 하늘을 납니다.
드론이 프로펠러로 공중에 떠오릅니다.
```

---

# 🧱 4. 인터페이스 vs 추상클래스 비교

학생들이 가장 헷갈리는 부분이라 **표로 깔끔하게 정리**해줄게.

| 구분 | 추상클래스 | 인터페이스 |
|------|-------------|-------------|
| 목적 | 공통된 속성과 기능 제공 + 일부 기능 강제 | “규칙(기능)”만 강제 |
| 메서드 | 일반 메서드 + 추상 메서드 모두 가능 | 기본적으로 추상 메서드 |
| 필드 | 일반 필드 가능 | 상수(public static final)만 가능 |
| 생성자 | 있음 | 없음 |
| 상속 | 단일 상속만 가능 | 다중 구현 가능 |
| 사용 상황 | 상속 계층 구조가 필요할 때 | 서로 다른 클래스에 공통 기능을 강제할 때 |
| 예 | 탈것(Vehicle), 동물(Animal) | 날기(Flyable), 저장 가능(Saveable), 클릭 가능(Clickable) |

---

# 🎯 5. 핵심 정리

### ✔ 추상클래스는 “공통된 틀 + 일부 미완성 기능”
- 상속 계층 구조를 만들 때 사용
- 공통 필드, 공통 메서드 제공 가능

### ✔ 인터페이스는 “기능 규칙만 정의”
- 서로 다른 클래스에 공통 기능을 강제할 때 사용
- 다중 구현 가능 → 유연한 설계

---

좋아 jong, 이 부분은 학생들이 인터페이스를 배울 때 꼭 짚고 넘어가야 하는 핵심 변화야.  
**Java 8 이후 인터페이스는 더 이상 “추상 메서드만 있는 설계도”가 아니다.**  
default 메서드와 static 메서드가 추가되면서 훨씬 강력해졌지.

아래에서 **개념 → 목적 → 예제 → 추상클래스와 비교**까지 깔끔하게 정리해줄게.

---

# 🧩 1. Java 8 이후 인터페이스의 변화

Java 8부터 인터페이스는 다음 3가지 종류의 메서드를 가질 수 있어:

1. **추상 메서드 (abstract method)**  
2. **디폴트 메서드 (default method)** ← Java 8 추가  
3. **정적 메서드 (static method)** ← Java 8 추가  

---

# 🟦 2. default 메서드란?

### ✔ 인터페이스에서 “기본 구현”을 제공하는 메서드  
즉, 인터페이스를 구현하는 클래스가 **반드시 오버라이딩할 필요가 없음**.

### ✔ 문법
```java
default void methodName() {
    // 기본 구현
}
```

### ✔ 왜 생겼을까?
Java 8에서 **람다식 + 스트림 API**가 도입되면서  
`List`, `Map` 같은 기존 인터페이스에 새로운 메서드를 추가해야 했어.

하지만 인터페이스에 메서드를 추가하면?

→ 기존 모든 구현 클래스가 컴파일 에러 발생  
→ 수천 개의 라이브러리가 동시에 깨짐

그래서 **기존 인터페이스에 새로운 기능을 추가하면서도  
구현 클래스에 영향을 주지 않기 위해** default 메서드가 도입됨.

---

# 🟦 3. static 메서드란?

### ✔ 인터페이스 자체에서 호출하는 유틸리티 메서드  
클래스의 static 메서드와 동일한 개념.

### ✔ 문법
```java
static void methodName() {
    // 구현
}
```

### ✔ 특징
- 인터페이스 이름으로 호출  
- 구현 클래스와 무관  
- 오버라이딩 불가

---

# 🚀 4. default / static 메서드 예제

## 🔹 인터페이스 정의

```java
interface Calculator {

    // 추상 메서드
    int add(int a, int b);

    // default 메서드
    default int subtract(int a, int b) {
        return a - b;
    }

    // static 메서드
    static int multiply(int a, int b) {
        return a * b;
    }
}
```

---

## 🔹 구현 클래스

```java
class MyCalculator implements Calculator {
    @Override
    public int add(int a, int b) {
        return a + b;
    }
}
```

---

## 🔹 테스트 코드

```java
public class Test {
    public static void main(String[] args) {
        Calculator c = new MyCalculator();

        System.out.println(c.add(5, 3));        // 8
        System.out.println(c.subtract(5, 3));   // 2 (default 메서드)
        System.out.println(Calculator.multiply(5, 3)); // 15 (static 메서드)
    }
}
```

---

# 🎯 5. default / static 메서드의 장점

### ✔ default 메서드
- 기존 인터페이스에 기능을 추가해도 구현 클래스가 깨지지 않음  
- 공통 기능을 인터페이스에서 제공 가능  
- 다중 인터페이스 구현 시 충돌 해결 가능

### ✔ static 메서드
- 인터페이스 관련 유틸리티 메서드를 인터페이스 내부에 둘 수 있음  
- 구현 클래스와 독립적

---

# 🧱 6. 추상클래스와 비교

| 구분 | 인터페이스 | 추상클래스 |
|------|-------------|-------------|
| 목적 | 규칙(행동) 정의 | 공통 속성 + 공통 기능 제공 |
| default 메서드 | 있음 | 일반 메서드로 이미 가능 |
| static 메서드 | 있음 | static 메서드 가능 |
| 필드 | 상수만 가능 | 일반 필드 가능 |
| 생성자 | 없음 | 있음 |
| 다중 상속 | 가능 | 불가능 |
| 사용 상황 | 능력(행동) 부여 | 상속 계층 구조 필요 |

---

# 🎉 핵심 요약

- **default 메서드**: 인터페이스에 “기본 구현”을 제공  
- **static 메서드**: 인터페이스 자체에서 호출하는 유틸리티 메서드  
- Java 8 이후 인터페이스는 “순수 추상 설계도”에서  
  **“기본 기능도 제공 가능한 유연한 설계도”** 로 진화함

---
