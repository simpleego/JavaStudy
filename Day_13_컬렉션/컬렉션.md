# 컬렉션
- 자바 컬렉션 프레임워크는 대표적인 데이터 구조를 객체화 해서 편리하게 사용할 수 있는 클래스들의 모임입니다.
- List
- Queue
- Set
- Map

---

## 📚 자바 컬렉션 정리

### 1. **List**
- **특징**: 순서가 있고, 중복 허용  
- **구현 클래스**: `ArrayList`, `LinkedList`, `Vector`  
- **사용 예시**: 학생 이름 목록, 순차적 데이터 저장  

---

### 2. **Queue**
- **특징**: FIFO(First-In-First-Out) 구조, 순서대로 처리  
- **구현 클래스**: `LinkedList`, `PriorityQueue`, `ArrayDeque`  
- **사용 예시**: 작업 대기열, 메시지 큐  

---

### 3. **Set**
- **특징**: 중복 허용하지 않음, 순서 없음  
- **구현 클래스**: `HashSet`, `LinkedHashSet`, `TreeSet`  
- **사용 예시**: 유일한 값 저장 (예: 회원 ID, 태그)  

---

### 4. **Map**
- **특징**: Key-Value 쌍으로 저장, Key는 중복 불가  
- **구현 클래스**: `HashMap`, `LinkedHashMap`, `TreeMap`, `Hashtable`  
- **사용 예시**: 사전(Dictionary), 설정값(Key=설정명, Value=값)  

---

## 💻 예제 코드

```java
import java.util.*;

public class CollectionExample {
    public static void main(String[] args) {
        // 1. List 예제
        List<String> list = new ArrayList<>();
        list.add("홍길동");
        list.add("이순신");
        list.add("홍길동"); // 중복 허용
        System.out.println("List: " + list);

        // 2. Queue 예제
        Queue<String> queue = new LinkedList<>();
        queue.add("작업1");
        queue.add("작업2");
        queue.add("작업3");
        System.out.println("Queue poll: " + queue.poll()); // FIFO → 작업1
        System.out.println("Queue: " + queue);

        // 3. Set 예제
        Set<String> set = new HashSet<>();
        set.add("사과");
        set.add("바나나");
        set.add("사과"); // 중복 제거됨
        System.out.println("Set: " + set);

        // 4. Map 예제
        Map<String, Integer> map = new HashMap<>();
        map.put("국어", 90);
        map.put("수학", 85);
        map.put("영어", 90);
        System.out.println("Map: " + map);
        System.out.println("수학 점수: " + map.get("수학"));
    }
}
```

---

## ⚡ 실행 결과 예시
```
List: [홍길동, 이순신, 홍길동]
Queue poll: 작업1
Queue: [작업2, 작업3]
Set: [바나나, 사과]
Map: {국어=90, 수학=85, 영어=90}
수학 점수: 85
```
